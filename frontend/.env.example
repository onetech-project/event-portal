# Copy to .env.local and adjust.
#
# Only NEXT_PUBLIC_* variables reach the browser bundle. Never put a secret here:
# anything prefixed NEXT_PUBLIC_ is embedded in the JavaScript that is shipped to
# every visitor.

# Base URL of the Go API, including the /api/v1 prefix.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/api/v1
